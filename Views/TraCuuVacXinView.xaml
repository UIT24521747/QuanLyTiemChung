<UserControl x:Class="QuanLyKhachHang.Views.TraCuuVacXinView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root"
             Background="#F0F0F0">
    <Grid Margin="22,16,22,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Tra Cứu Vắc-xin"
                   FontSize="28" FontWeight="Bold" Foreground="#1F3045" Margin="0,0,0,14"/>

        <!-- Row 1: Mã vắc-xin | Tên vắc-xin | Loại vắc-xin -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Mã vắc-xin" Style="{StaticResource FieldLabelStyle}"/>
                <TextBox x:Name="txtMaVacXin" Style="{StaticResource InputStyle}" Tag="Có chứa"/>
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Text="Tên vắc-xin" Style="{StaticResource FieldLabelStyle}"/>
                <TextBox x:Name="txtTenVacXin" Style="{StaticResource InputStyle}" Tag="Có chứa"/>
            </StackPanel>
            <StackPanel Grid.Column="4">
                <TextBlock Text="Loại vắc-xin" Style="{StaticResource FieldLabelStyle}"/>
                <ComboBox x:Name="cboLoaiVacXin" Style="{StaticResource ComboStyle}"
                          DisplayMemberPath="TenLoaiVacXin"/>
            </StackPanel>
        </Grid>

        <!-- Row 2: Khoảng cách từ/đến | Số lượng tồn từ/đến -->
        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Khoảng cách giữa các mũi từ" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center" Margin="0,0,0,0"/>
            <TextBox Grid.Column="2" x:Name="txtKCTu" Style="{StaticResource InputStyle}"/>
            <TextBlock Grid.Column="4" Text="đến" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center" Margin="0,0,0,0"/>
            <TextBox Grid.Column="6" x:Name="txtKCDen" Style="{StaticResource InputStyle}"/>
            <TextBlock Grid.Column="8" Text="Số lượng tồn từ" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center" Margin="0,0,0,0"/>
            <TextBox Grid.Column="10" x:Name="txtSLTonTu" Style="{StaticResource InputStyle}"/>
            <TextBlock Grid.Column="12" Text="đến" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center" Margin="0,0,0,0"/>
            <TextBox Grid.Column="14" x:Name="txtSLTonDen" Style="{StaticResource InputStyle}"/>
        </Grid>

        <!-- Row 3: Mã lô (combo) | Hãng sản xuất -->
        <Grid Grid.Row="3" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Mã lô" Style="{StaticResource FieldLabelStyle}"/>
                <ComboBox x:Name="cboMaLo" Style="{StaticResource ComboStyle}"
                          SelectionChanged="CboMaLo_SelectionChanged"/>
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Text="Hãng sản xuất" Style="{StaticResource FieldLabelStyle}"/>
                <TextBox x:Name="txtHangSanXuat" Style="{StaticResource InputStyle}" Tag="Có chứa"/>
            </StackPanel>
        </Grid>

        <!-- Row 4: Số lượng nhập từ/đến | Đơn giá nhập từ/đến -->
        <Grid Grid.Row="4" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Số lượng nhập từ" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center" Margin="0,0,0,0"/>
            <TextBox Grid.Column="2" x:Name="txtSLNhapTu" Style="{StaticResource InputStyle}"/>
            <TextBlock Grid.Column="4" Text="đến" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center" Margin="0,0,0,0"/>
            <TextBox Grid.Column="6" x:Name="txtSLNhapDen" Style="{StaticResource InputStyle}"/>
            <TextBlock Grid.Column="8" Text="Đơn giá nhập từ" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center" Margin="0,0,0,0"/>
            <TextBox Grid.Column="10" x:Name="txtDonGiaTu" Style="{StaticResource InputStyle}"/>
            <TextBlock Grid.Column="12" Text="đến" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center" Margin="0,0,0,0"/>
            <TextBox Grid.Column="14" x:Name="txtDonGiaDen" Style="{StaticResource InputStyle}"/>
        </Grid>

        <!-- Row 5: Ngày hết hạn từ/đến -->
        <Grid Grid.Row="5" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Ngày hết hạn từ" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center" Margin="0,0,0,0"/>
            <DatePicker Grid.Column="2" x:Name="dpNHHTu" Style="{StaticResource DateStyle}"/>
            <TextBlock Grid.Column="4" Text="đến" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center" Margin="0,0,0,0"/>
            <DatePicker Grid.Column="6" x:Name="dpNHHDen" Style="{StaticResource DateStyle}"/>
        </Grid>

        <!-- Row 6: Nhà cung cấp -->
        <StackPanel Grid.Row="6" Margin="0,0,0,10">
            <TextBlock Text="Nhà cung cấp" Style="{StaticResource FieldLabelStyle}"/>
            <TextBox x:Name="txtNhaCungCap" Style="{StaticResource InputStyle}" Tag="Có chứa"/>
        </StackPanel>

        <!-- Row 7: Mã phiếu nhập từ/đến -->
        <Grid Grid.Row="7" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Mã phiếu nhập từ" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center" Margin="0,0,0,0"/>
            <TextBox Grid.Column="2" x:Name="txtMPNTu" Style="{StaticResource InputStyle}"/>
            <TextBlock Grid.Column="4" Text="đến" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center" Margin="0,0,0,0"/>
            <TextBox Grid.Column="6" x:Name="txtMPNDen" Style="{StaticResource InputStyle}"/>
        </Grid>

        <!-- Row 8: Ngày nhập từ/đến -->
        <Grid Grid.Row="8" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Ngày nhập từ" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center" Margin="0,0,0,0"/>
            <DatePicker Grid.Column="2" x:Name="dpNgayNhapTu" Style="{StaticResource DateStyle}"/>
            <TextBlock Grid.Column="4" Text="đến" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center" Margin="0,0,0,0"/>
            <DatePicker Grid.Column="6" x:Name="dpNgayNhapDen" Style="{StaticResource DateStyle}"/>
        </Grid>

        <!-- Row 9: Buttons -->
        <StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,4,0,12">
            <Button Content="🔍  Tra cứu" Click="BtnTraCuu_Click"
                    Style="{StaticResource ActionButtonStyle}" Padding="20,9" Margin="0,0,10,0" Width="130"/>
            <Button Content="→  Thoát" Click="BtnThoat_Click"
                    Style="{StaticResource ActionButtonStyle}" Padding="20,9" Margin="0" Width="100"/>
        </StackPanel>

        <!-- Row 10: Results DataGrid -->
        <Border Grid.Row="10" Background="White" BorderBrush="#C7D3E8" BorderThickness="1" CornerRadius="6" ClipToBounds="True">
            <DataGrid x:Name="dgResults"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserSortColumns="True"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      GridLinesVisibility="All"
                      HeadersVisibility="Column"
                      Background="White"
                      RowBackground="White"
                      AlternatingRowBackground="#F8FAFC"
                      BorderThickness="0"
                      FontSize="13"
                      RowHeight="32">
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="Padding" Value="0"/>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.Columns>

                    <!-- STT -->
                    <DataGridTemplateColumn Header="STT" Width="50" IsReadOnly="True">
                        <DataGridTemplateColumn.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                            </Style>
                        </DataGridTemplateColumn.CellStyle>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="#E9EEF6">
                                    <TextBlock Text="{Binding Stt}" FontSize="13"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Tên vắc-xin" Binding="{Binding TenVacXin}" Width="*" MinWidth="140">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Padding" Value="6,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Loại vắc-xin" Binding="{Binding TenLoaiVacXin}" Width="110">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Padding" Value="6,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Khoảng cách giữa các mũi" Binding="{Binding KhoangCachGiuaCacMui}" Width="180">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="Padding" Value="6,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Số lượng tồn" Binding="{Binding SoLuongTon}" Width="110">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="Padding" Value="6,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
